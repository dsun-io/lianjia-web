# 🧭 开发规范（新增 / 修改页面必读）

> 本文件是联佳官网的工程约定底线。**任何新增页面或改动导航/结构的工作，动手前必须读完本文件，完成后必须按"同步清单"逐项核对。**
>
> 站点是纯静态 + Swup 内联跳转 + Tailwind(purge 编译产物) 的组合，很多约定不写下来就会踩坑（导航不一致、样式丢失、跳转退化成整页刷新、SEO 漏配）。

---

## 0. 元规范：文档必须与代码同步

**改了代码 / 加了页面 / 调了约定，就必须同步更新相关说明文件**，否则文档会比代码更误导人。至少检查：

- `README.md` —— 文件结构、页面清单、功能说明
- `开发规范.md`（本文件）—— 若引入了新的工程约定
- `sitemap.xml` —— 新增可索引页面
- 相关的 `.md` 方案/清单文件（`建站技术方案.md` / `素材清单.md` / `待确认数据清单.md`）

> 过时的文档比没有文档更糟。提交前问自己一句："我这次的改动，有没有让某个说明文件变得不准确？"

---

## 1. 新增页面同步清单（逐项核对）

新增任意页面（产品页 / 博客 / 政策页…）时，**整页克隆现有标准骨架**，再逐项过下面的清单：

- [ ] **整页克隆标准骨架**：head 脚本块 + design tokens `<style>` + `<main id="swup">` 容器 + footer + 浮动按钮 + 末尾脚本（AOS/tracker/cookie-consent），一字不少。保证 Swup 内联跳转与视觉一致。
- [ ] **顶部导航是全站持久元素**：`<header>` 在 `#swup` 容器**外**，Swup 跳转时不刷新。所以**新增/调整导航项必须同步改所有"标准骨架"页面**（见 §2），否则从 A 页跳到 B 页时顶栏显示的是 A 页的旧导航。
- [ ] **底部询盘表单全站统一（不得自创样式）**：所有带底部询盘表单的页面，一律复用**首页 `#contact` 的标准表单**（slate-800 两栏：左表单 + 右联系卡/地图，提交到同一 Google Apps Script，含蜜罐 `_gotcha`、`#inquiry-success` 成功态）。
  - **唯一允许按页面定制的是顶部引导文案**（左栏的 `<h2>` 标题 + 一句话引导语）；表单字段、样式、成功态、联系卡一字不改。
  - 非首页 section 用 `id="inquiry"`（首页用 `id="contact"`），以配合 `swup-init.js` 对顶栏 "Get a Quote" 的指向。
  - `source_page` 隐藏字段设为该页 slug（如 `blog-index`、`blog-hinge-joint`），便于后端区分来源。
  - 表单本体保留 `id="inquiry-form"` 及各字段 id，确保 `form-handler.js` 正常绑定。
  - ❌ 不要用"两个按钮的 CTA 引导带"等特殊样式替代标准表单。
- [ ] **链接路径规范**：
  - 跨页超链接 / 导航 / footer 一律带 **`.html`**（GitHub Pages 没有无扩展名回退，干净 URL 会 404）。目录首页例外，用 `/blog/` 这种目录形式（GitHub Pages 自动返回 `index.html`）。
  - `canonical` 用**干净无扩展名** URL（如 `/blog/hinge-joint-vs-ring-lock-field-fence`、`/blog/`），与现有产品页口径一致。
  - 资产一律 `/assets/...` 绝对路径（子目录页面也能正确解析）。
- [ ] **Tailwind 是 purge 编译产物**：`tailwind.config.js` 的 `content` 只扫描已登记的路径。
  - 新页**优先复用现有页面已出现的 class**（不重编译也有样式）。
  - 新增目录（如 `blog/`）**必须把 `./blog/*.html` 加进 `content`**，并用 standalone CLI 重新编译 `assets/css/tailwind.css`。
  - JS 动态加的 class 要进 `safelist`。
- [ ] **登记进工具脚本**（否则被静默跳过）：
  - `check-assets.js` 的 `PAGES` 数组 —— 加入新页面路径
  - `fix-paths.js` 的 `files` 数组 —— 加入新页面（支持子目录部署时的路径改写）
- [ ] **SEO 收尾**：
  - 每页 `title` / `meta description` / `meta keywords` / `canonical` / Open Graph
  - `sitemap.xml` 增 `<loc>` + `<lastmod>`
  - 结构化数据：产品页 `Product`、博客文章 `BlogPosting`、博客列表 `Blog`。**禁止 fake review / aggregateRating**（沿用现有红线）。
- [ ] **内容合规**：
  - 当地标准只说「available … on request / supply to … when specified」，**绝不声称已认证**。   
  - 不编造价格、客户案例、认证、工厂数据。缺真实素材就留 `TODO` 占位或不写。
  - 文案 AU 拼写优先（galvanised）。

---

## 2. "标准骨架"页面清单（导航同步范围）

下列页面共享同一顶部导航与 footer，**任何导航/页脚改动必须全部同步**：

| 页面 | 是否含标准 header/Swup |
|---|---|
| `index.html` | ✅ |
| `products/field-fence.html` | ✅ |
| `products/chain-link-fence.html` | ✅ |
| `products/y-post.html` | ✅ |
| `blog/index.html` | ✅ |
| `blog/*.html`（各文章页） | ✅ |
| `privacy.html` | ✅ |
| `thank-you.html` | ❌ 极简表单成功页，无标准 header/Swup，**豁免** |

> 当前顶栏顺序（统一）：`Home │ Products▾ │ Blog │ Why Us │ Connect │ FAQ │ Contact`。`Products▾` 下拉只装产品详情页；`Blog` 是独立一级入口，**不得放进 Products 下拉**。

---

## 3. 本地 dev server 行为（与 GitHub Pages 对齐）

`server.js`（端口 8080）已对齐生产环境的两种路由：

1. **目录索引**：`/blog/` → `blog/index.html`（与 GitHub Pages 一致）
2. **无扩展名回退**：`/products/field-fence` → `products/field-fence.html`（仅本地便利；GitHub Pages 无此能力，故站内链接仍须带 `.html`）

改动验证：`node check-assets.js` 跑通全部 `PAGES` 的资产引用零 404。

---

*本规范随项目演进持续更新；新增约定请追加于此，并在 `README.md` 留指针。*
