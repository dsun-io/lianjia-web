# 联佳官网建站技术方案

## Context

基于说明书《联佳官网建站说明书（发给 AI 直接做站）》，为河北安平钢丝围栏工厂建设一页式高端 B2B 外贸展示站。目标客户：澳大利亚 / 新西兰的围栏批发商、工程商、进口商。核心目标——30 秒内打消「这家中国厂是真的吗」的疑虑，促成询盘。

## 技术选型

| 决策 | 选择 | 理由 |
| --- | --- | --- |
| CSS | Tailwind v3 Play CDN（生产用 standalone CLI） | 工具类优先、响应式友好、AI 生成效率高 |
| 构建 | 无构建步骤 | 单 HTML 文件 + 独立产品页，push 即部署 |
| 动画 | CSS @keyframes + AOS（约 14KB） | 轻量、声明式、易生成 |
| 交互 | 自定义光标系统 + 磁力按钮 + 视差滚动 | 工业主题沉浸感，桌面端增强体验 |
| 表单 | Google Apps Script + Google Sheet | 零成本、数据落库、邮件通知、配额预警 |
| 埋点 | LJTracker（停留时长追踪） | 追踪各产品页停留秒数，提交时一并传给后端 |
| WhatsApp | wa.me 链接 | 零依赖、全平台可用 |
| 视频 | video 标签 + poster + 移动端降级 | 首屏快、优雅降级 |
| 托管 | GitHub Pages / Netlify | 免费、自动部署 |

> **⚠️ Tailwind 版本坑**：Tailwind v4 没有像 v3 那样成熟的 Play CDN，浏览器直引较重。当前使用 v3 Play CDN（最稳），生产上线再用 standalone CLI 出精简 CSS。

## 文件结构

```
外贸独立站/
├── index.html                        # 主页（单页式，含所有区块）
├── thank-you.html                    # 提交成功页（备用）
├── form-handler.gs                   # Apps Script 表单后端（Sheet 兜底 + 邮件 + 配额预警）
├── products/                         # 产品详情页
│   ├── field-fence.html              # Field Fence 详情
│   ├── chain-link-fence.html         # Chain Link Fence 详情
│   └── y-post.html                   # Y Post / Star Picket 详情
├── assets/
│   ├── img/
│   │   ├── logo.svg
│   │   ├── og-cover.svg
│   │   ├── favicon.svg
│   │   ├── hero-poster.svg
│   │   ├── field-fence-01..03.jpg
│   │   ├── chain-link-01..03.jpg
│   │   ├── y-post-01..03.jpg
│   │   ├── barbed-wire-01..03.svg
│   │   ├── factory-01..02.jpg
│   │   ├── cert-01..02.jpg
│   │   └── green-post-1..19.jpg      # 花边立柱素材（备用）
│   ├── video/
│   │   ├── hero-factory.mp4
│   │   └── factory-tour.mp4
│   ├── gif/
│   │   ├── weaving.gif
│   │   └── galvanizing.gif
│   └── js/
│       ├── tracker.js                # LJTracker 核心追踪器
│       ├── time-tracker.js           # 停留时长追踪
│       └── cookie-consent.js         # Cookie 同意横幅
├── favicon.svg
├── robots.txt
└── sitemap.xml
```

> HTML 内引用一律用**相对路径**（`assets/img/...`），不要前导斜杠 `/assets/...`，也不要写死域名——迁到 GitHub Pages 子目录或换域名都不会 404。

## 页面结构（13 节 = Head + 11 个内容区 + 浮动元素）

### 0. HTML Head

- `<html lang="en">`、`<meta charset="UTF-8">`、viewport、description、keywords
- Open Graph 标签（title / description / image / url）
- JSON-LD 结构化数据（Organization + Product）——**绝不编造 aggregateRating / review 星级**
- Tailwind CDN（开发阶段）
- AOS CSS
- 自定义 CSS（配色变量 + 自定义动画）

### 1. 吸顶导航 Header

- 左：Logo（`assets/img/logo.svg`）
- 中：Home | Products（下拉菜单：Field Fence / Chain Link Fence / Y Post）| Why Us | Factory | FAQ | Contact（锚点链接）
- 右：Get a Quote 按钮 + WhatsApp 图标
- 滚动固定吸顶、半透明毛玻璃背景
- 移动端：汉堡菜单（含 Products 子菜单展开）
- 移动端底部固定栏：WhatsApp + Get a Quote 双按钮

### 2. Hero 首屏

- 视频背景（`assets/video/hero-factory.mp4`，静音自动循环）+ 深色半透明遮罩
- Eyebrow：`ANPING, CHINA · WIRE FENCE MANUFACTURER SINCE [YEAR_FOUNDED]`
- 主标题（A 版）：`Field Fence, Chain Link & Y Posts — Factory Direct from Anping, China`
- 副标题：完整文案见说明书第五节
- 双按钮：Get a Free Quote（主） + View Product Catalog（次）
- 移动端：视频降级为静态 `hero-poster.svg`
- 桌面端：自定义工业十字准星光标 + 焊接火花光晕效果 + 文字视差滚动

### 3. 承诺条 Offer Bar

- 5 个承诺横排：`Factory-Direct Pricing · Free Samples · Quote in 24 Hours · MOQ 1x20GP · Ships to AU & NZ`
- 图标 + 文字，浅色背景条

### 4. 信任条 Trust Strip

5 个数据（数字滚动动画）：
- `[YEARS]+ Years Manufacturing`
- `[CAPACITY]+ Containers / Month`
- `Hot-Dipped Galvanized`
- `Factory Audit Welcome`
- `Shipping to AU & NZ`

### 5. 产品区 Products（4 个产品）

每个产品左图右文（移动端上图下文），产品 2/4 左右反转：
- 3 张图 + 灯箱点击放大（自定义 lightbox 实现）
- 右侧：标题 + 一句卖点 + 3 条要点 + 规格表 + Request a Quote + View Full Details 链接
- **Product 1 — Field Fence（Hinge Joint & Ring Lock）**：Hero 图 + 两种结型卡片切换（Hinge Joint / Ring Lock），点击切换主图；右侧 3 条卖点 + 规格表（Material / Knot Types / Height / Zinc Coating / Roll Length / MOQ）+ CTA
- **Product 2 — Chain Link Fence**：规格表（Material / Wire Diameter / Mesh Opening / Height / Roll Length / Surface / Color / Accessories）
- **Product 3 — Y Post / Star Picket**：三种类型卡片切换（Studded / Plain / Scalloped）+ 四列规格表（Length / Plain kg / Scalloped kg / Studded kg）
- **Product 4 — Barbed Wire**：规格表（Material / Wire Diameter / Strand Type / Barb Spacing / Zinc Coating / Roll Length / Packing / MOQ）
- 每个产品均有独立详情页（`products/field-fence.html` 等）
- 完整文案和规格数据来自说明书第五节

### 6. Why Choose Us（6 卡片）

- 3×2 网格；每张：图标 + 粗标题 + 一句话
- Real Factory / Hot-Dipped Galvanized / Custom Specs / Full QC / Reliable Lead Times / Focused on AU&NZ

### 7. Factory & Process

- 6 步：Wire Drawing → Galvanizing/Coating → Weaving & Welding → In-line QC → Export Packing → Container Loading
- 嵌入工厂视频（`assets/video/factory-tour.mp4`）
- 验厂邀请文案

### 8. About Us

- 完整公司简介（占位符版）

### 9. FAQ（手风琴折叠）

- 8 个 Q&A，文案取自说明书第五节第 9 区
- 包含额外的价格 FAQ：「Is the website price the final price?」
- 点击展开/收起，同时只展开一个

### 10. 询盘表单 Inquiry

- 左：表单（Name*、Email*、Company、Country、Product 下拉、Scenario、Fence Length、Need Posts、Message）
- 右：联系方式 + 24h 回复承诺 + 询盘成功后的 WhatsApp 快捷入口
- 移动端表单在上
- **后端：Google Apps Script**（`form-handler.gs`）
  - 表单 action 直接指向 Apps Script 部署 URL
  - **蜜罐检测**：`_gotcha` 隐藏字段，机器人填值后静默丢弃
  - **先写 Sheet 再发邮件**：配额耗尽也不丢客户数据
  - **配额预警**：`MailApp.getRemainingDailyQuota()` < 10 时企业微信 webhook 报警
  - **Turnstile 校验**：预留接口，配置 secret 后自动生效
- **前端**：内联提交（fetch + FormData），提交成功显示 Thank You 消息（不跳转页面）+ URL 变为 `#thank-you`，支持浏览器前进/后退
- **停留时长埋点**：`tracker.js` + `time-tracker.js` 追踪各产品页停留秒数，提交时一并传给后端
- **来源追踪**：自动检测用户来自哪个产品页，自动选中对应产品
- 通知邮箱发往 `dspro0124@163.com`

### 11. 页脚 Footer

- 公司名 + 产品列表 + 联系方式 + 版权 `© [YEAR] [COMPANY_NAME]`

### 12. 浮动元素（所有页面统一）

所有页面（首页 + 3 个产品详情页）均包含相同的浮动元素：
- 右下角：Back to Top 悬浮按钮（滚动超过 400px 显示，白色底 + 橙色边框）
- 右下角：Get a Quote 悬浮按钮（信封图标）
- 右下角：WhatsApp 悬浮按钮（用 `[WHATSAPP_LINK]`，带预填消息 + 脉冲动效）
- 移动端底部固定栏：WhatsApp + Get a Quote 双按钮（`sm:hidden`）
- 桌面端浮动按钮 `hidden sm:flex`，Back to Top 位于最上方（`bottom-40`）

## 交互系统（桌面端增强）

### 自定义光标系统

- **工业十字准星光标**：全站隐藏系统默认光标（`cursor: none`），用橙色十字线 + 中心圆点替代
- **悬停状态**：鼠标悬停在可交互元素上时，十字线收缩为 40px 实心橙色圆圈 + 发光阴影
- **焊接火花光晕**：Hero 区域内鼠标跟随的橙色径向渐变光晕
- **触屏设备自动禁用**：`@media (hover: none) and (pointer: coarse)` 恢复系统默认光标
- **系统指针隐藏策略**：4 层防护确保系统指针不显示——
  1. `<html style="cursor:none">`：浏览器最早解析
  2. `<head>` 内联 `<style>`：在 Tailwind 等外部 CSS 加载前生效
  3. `<body>` 内联 JS `mousedown` 事件：拦截链接点击，跳转前隐藏光标
  4. `beforeunload` 事件兜底
- **⚠️ 已知问题**：跨页面跳转瞬间仍会短暂显示系统默认指针，疑似浏览器导航过渡行为，待进一步研究（SPA 化或 Page Transition API）

### 磁力按钮效果

- `.magnetic-btn` 类的按钮在鼠标靠近时产生磁力吸附效果
- 鼠标距离越远，偏移越大（最大 8px）
- 离开时平滑回弹

### 视差滚动

- Hero 区域内的 `[data-parallax]` 元素跟随鼠标产生微位移
- 不同元素设置不同系数（0.02 ~ 0.05）
- 鼠标离开 Hero 区域时元素平滑回弹

### 点击波纹效果

- 全局点击时产生橙色圆环波纹（wire mesh 主题）
- 600ms 自动消失

### 产品类型卡片（统一设计语言）

所有含多变体的产品均使用**相同交互模式**：Hero 大图 + 可点击类型卡片缩略图，点击切换主图 + 高亮选中卡片。

- **Y Post**：三种类型（Studded / Plain / Scalloped）卡片切换
- **Field Fence**：两种结型（Hinge Joint / Ring Lock）卡片切换
- 选中态：`border-orange-500` + `bg-orange-50/50` + 标题变橙色

## 配色方案

```css
:root {
  --steel-gray: #2D3436;
  --deep-blue:  #1B2838;
  --accent:     #E67E22;
  --white:      #FFFFFF;
  --light-gray: #F8F9FA;
  --text-gray:  #6B7280;
}
```

## 动画方案

| 元素 | 动画 | 实现 |
| --- | --- | --- |
| Hero 文字 | 淡入上移 | CSS @keyframes fadeInUp |
| 各区块 | 滚动淡入 | AOS data-aos="fade-up" |
| 信任条数字 | 0 跳动到目标值 | JS Intersection Observer |
| 产品卡片 | 悬停放大 + 阴影 | CSS transition |
| 产品图 | 点击放大灯箱 | 自定义 lightbox（键盘 ESC/方向键支持） |
| WhatsApp 按钮 | 轻微脉冲 | CSS @keyframes wa-pulse |
| 自定义光标 | 鼠标跟随 + 悬停变形 | JS requestAnimationFrame + transform |
| 焊接光晕 | 鼠标跟随光晕 | JS + CSS radial-gradient + mix-blend-mode |
| 磁力按钮 | 鼠标靠近吸附 | JS mousemove + transform |
| 视差滚动 | 鼠标位移联动 | JS + data-parallax 属性 |
| 点击波纹 | 橙色圆环扩散 | JS + CSS @keyframes ripple-expand |
| 提交成功 | 勾选动画 | CSS @keyframes checkDraw + stroke-dashoffset |

## 占位符系统

| 占位符 | 代表 |
| --- | --- |
| `[COMPANY_NAME]` | 公司英文名 |
| `[DOMAIN]` | 域名 |
| `[EMAIL]` | 销售邮箱（= sales@[DOMAIN]） |
| `[PHONE]` | 电话 |
| `[WHATSAPP]` | WhatsApp 号（带国家码） |
| `[WHATSAPP_LINK]` | https://wa.me/号码 |
| `[YEAR_FOUNDED]` | 成立年份 |
| `[YEARS]` | 生产年数 |
| `[CAPACITY]` | 月产能（柜/月） |
| `[EMPLOYEES]` | 员工数 |
| `[EXPORT_COUNTRIES]` | 出口国家数 |
| `[YEAR]` | 页脚版权年份 |

## 安全加固

| 防护层 | 措施 | 状态 |
|--------|------|------|
| 蜜罐 | `_gotcha` 隐藏字段，前端+后端双重校验 | ✅ 已部署 |
| Turnstile | Cloudflare 人机验证，配置 site_key 后自动显示 | ✅ 代码就位 |
| Sheet 兜底 | 先写 Sheet 再发邮件，数据不丢 | ✅ 已部署 |
| 配额预警 | 企业微信 webhook 报警（阈值 10 封） | ✅ 已配置 |
| GitHub Security | Dependabot alerts + security updates + malware alerts | ✅ 已开启 |
| 来源校验 | Apps Script 限制，靠蜜罐+Turnstile 兜底 | ⚠️ 尽力而为 |

## 素材与性能优化（待素材就绪后实施）

### 图片优化

- 格式：JPG/PNG → **WebP**（体积减少 25-35%）
- 懒加载：`loading="lazy"` + `decoding="async"`
- 响应式：`srcset` + `sizes` 按设备宽度加载不同尺寸
- 工具：`cwebp` 或 Squoosh 在线压缩

### GIF 优化

- GIF → **MP4**（体积减少 80-90%）
- 实现：`<video autoplay loop muted playsinline>` 替代 `<img>`
- 工具：`ffmpeg -i input.gif -vf "scale=trunc(iw/2)*2:trunc(ih/2)*2" -c:v libx264 -pix_fmt yuv420p -movflags +faststart output.mp4`

### 长视频方案

- **推荐：YouTube Unlisted 嵌入**
  - 优势：零带宽消耗、自适应码率、全球 CDN、SEO 加分
  - 实现：`<iframe src="https://www.youtube-nocookie.com/embed/VIDEO_ID">`
  - 设置：Unlisted（不公开搜索但可嵌入）+ `nocookie` 域名（减少追踪）
  - 加速：使用 `facade` 模式（点击后才加载 iframe，首屏零开销）
- **不推荐：直接上传到 GitHub Pages**
  - 文件限制 100MB、无自适应码率、无 CDN 加速、占用仓库空间

## 验收标准

- [x] 12 个内容区块全部呈现（含 Barbed Wire），外加 Head 与浮动元素
- [x] 文案与说明书一致
- [x] 占位符系统完整，无硬编码真实信息
- [x] 移动端响应式正常，首屏 3 秒内加载
- [x] 表单可提交（Google Apps Script 测试），且收到自动回执邮件
- [x] WhatsApp 链接可点击跳转
- [x] 所有图片 / 视频用相对路径
- [x] 全英文，无中文残留
- [x] 每张图有 alt 属性
- [x] 页面有 title / description / OG 标签
- [x] JSON-LD 无虚假评分
- [x] 产品详情页可正常访问
- [x] 灯箱可正常打开/关闭/切换
- [x] FAQ 手风琴可正常展开/收起
- [x] 自定义光标在桌面端正常显示，触屏设备自动禁用
- [ ] 跨页面跳转瞬间系统指针短暂显示（浏览器导航过渡行为，待解决）
- [x] Y Post 类型卡片可正常切换
- [x] Field Fence 类型卡片可正常切换（Hinge Joint / Ring Lock）
- [x] Field Fence 详情页变体选择器、对比表、Tab 规格表正常工作
- [x] 所有页面（首页 + 3 个详情页）均有 Back to Top 按钮
- [x] 设计语言统一：Field Fence 与 Y Post 使用相同的 Hero + 卡片切换模式
